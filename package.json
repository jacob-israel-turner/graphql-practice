{
  "name": "graphql",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "babel": {
    "presets": ["latest"]
  },
  "scripts": {
    "start": "react-scripts start",
    "server": "babel-node ./server/index.js",
    "server:watch": "nodemon ./server/index.js --exec babel-node",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
